import {prisma} from "@/lib/prisma";import {Header,Status} from "@/components/ui";export default async function Page(){const sends=await prisma.birthdaySend.findMany({include:{employee:true},orderBy:{createdAt:"desc"},take:100});return <><Header title="Histórico de envios" description="Consulte os resultados e erros das mensagens."/><div className="card overflow-auto"><table className="table"><thead><tr><th>Colaborador</th><th>Aniversário</th><th>Envio</th><th>Status</th><th>Assunto / Erro</th></tr></thead><tbody>{sends.map(s=><tr key={s.id}><td><b>{s.employee.nome}</b><div className="text-xs">{s.emailDestino}</div></td><td>{new Intl.DateTimeFormat("pt-BR",{timeZone:"UTC"}).format(s.dataAniversario)}</td><td>{s.sentAt?new Intl.DateTimeFormat("pt-BR",{dateStyle:"short",timeStyle:"short"}).format(s.sentAt):"—"}</td><td><Status value={s.status}/></td><td>{s.assunto}<div className="text-xs text-red-600">{s.errorMessage}</div></td></tr>)}</tbody></table>{!sends.length&&<p className="p-10 text-center text-slate-500">Ainda não há envios registrados.</p>}</div></>}

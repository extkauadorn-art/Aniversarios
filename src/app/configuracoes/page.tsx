import {prisma} from "@/lib/prisma";import {Header} from "@/components/ui";import {SettingsForm} from "@/components/settings-form";export default async function Page(){const settings=await prisma.settings.findUniqueOrThrow({where:{id:"singleton"}});return <><Header title="Configurações" description="Personalize automação, remetente e regras do sistema."/><SettingsForm settings={settings}/></>}

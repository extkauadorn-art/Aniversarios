import {prisma} from "@/lib/prisma";import {Header} from "@/components/ui";export default async function Page(){const s=await prisma.settings.findUniqueOrThrow({where:{id:"singleton"}});return <><Header title="Templates" description="Modelo usado nas mensagens de aniversário."/><div className="card p-6"><p className="text-xs text-slate-500 mb-2">ASSUNTO</p><h2 className="font-bold text-lg">{s.subjectTemplate}</h2><hr className="my-5"/><p className="whitespace-pre-wrap leading-7">{s.bodyTemplate}</p><div className="mt-6 text-sm bg-blue-50 text-blue-800 rounded-lg p-4">Variáveis disponíveis: <code>{"{{nome}}, {{area}}, {{equipe}}, {{dataAniversario}}"}</code></div></div></>}

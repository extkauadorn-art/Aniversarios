import {prisma} from "@/lib/prisma";import {settingsSchema} from "@/lib/validation";import {NextRequest,NextResponse} from "next/server";export async function GET(){return NextResponse.json(await prisma.settings.findUnique({where:{id:"singleton"}}))}export async function PUT(req:NextRequest){const parsed=settingsSchema.safeParse(await req.json());if(!parsed.success)return NextResponse.json({erro:parsed.error.flatten()},{status:400});return NextResponse.json(await prisma.settings.update({where:{id:"singleton"},data:{...parsed.data,replyTo:parsed.data.replyTo||null,adminEmail:parsed.data.adminEmail||null}}))}

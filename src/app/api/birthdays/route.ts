import {prisma} from "@/lib/prisma";import {NextRequest,NextResponse} from "next/server";export async function GET(req:NextRequest){const month=Number(req.nextUrl.searchParams.get("month")||new Date().getUTCMonth()+1);const all=await prisma.employee.findMany({where:{ativo:true},include:{sends:true}});return NextResponse.json(all.filter(e=>e.dataNascimento.getUTCMonth()+1===month))}

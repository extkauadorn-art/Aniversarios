import {sendBirthday} from "@/lib/send";import {NextRequest,NextResponse} from "next/server";import {auth} from "@/auth";export async function POST(req:NextRequest,{params}:{params:Promise<{id:string}>}){if(!await auth())return NextResponse.json({erro:"Não autorizado"},{status:401});const result=await sendBirthday((await params).id,{automatic:false});if(req.headers.get("accept")?.includes("application/json"))return NextResponse.json(result);return NextResponse.redirect(new URL("/historico",req.url),303)}

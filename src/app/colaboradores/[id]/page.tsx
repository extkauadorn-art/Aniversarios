import {prisma} from "@/lib/prisma";import {Header} from "@/components/ui";import {EmployeeForm} from "@/components/employee-form";export default async function Page({params}:{params:Promise<{id:string}>}){const employee=await prisma.employee.findUniqueOrThrow({where:{id:(await params).id}});return <><Header title="Editar colaborador" description="Atualize os dados cadastrais."/><EmployeeForm employee={{...employee,dataNascimento:employee.dataNascimento.toISOString()}}/></>}

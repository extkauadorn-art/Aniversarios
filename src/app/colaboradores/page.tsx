import {prisma} from "@/lib/prisma";import {Header} from "@/components/ui";import Link from "next/link";export default async function Page({searchParams}:{searchParams:Promise<Record<string,string>>}){const q=await searchParams;const employees=await prisma.employee.findMany({where:{nome:q.busca?{contains:q.busca,mode:"insensitive"}:undefined,area:q.area||undefined,ativo:q.status?q.status==="ativos":undefined},orderBy:{nome:"asc"}});return <><Header title="Colaboradores" description="Cadastre e mantenha os dados da equipe." action={<Link className="btn" href="/colaboradores/novo">+ Novo colaborador</Link>}/><form className="card p-4 mb-5 flex gap-3"><input className="input" name="busca" placeholder="Pesquisar por nome..." defaultValue={q.busca}/><button className="btn-secondary btn">Filtrar</button></form><div className="card overflow-auto"><table className="table"><thead><tr><th>Nome</th><th>Aniversário</th><th>Área / Equipe</th><th>Contato</th><th>Status</th><th></th></tr></thead><tbody>{employees.map(e=><tr key={e.id}><td><b>{e.nome}</b><div className="text-xs text-slate-500">{e.cargo}</div></td><td>{new Intl.DateTimeFormat("pt-BR",{timeZone:"UTC"}).format(e.dataNascimento)}</td><td>{e.area||"—"}<div className="text-xs">{e.equipe}</div></td><td>{e.email||"Sem e-mail"}</td><td><span className={`badge ${e.ativo?"bg-emerald-100 text-emerald-700":"bg-slate-100"}`}>{e.ativo?"ATIVO":"INATIVO"}</span></td><td><Link className="text-blue-600" href={`/colaboradores/${e.id}`}>Editar</Link></td></tr>)}</tbody></table></div></>}
